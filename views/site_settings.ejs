<!--
  site_settings.ejs
  Site Settings Page
  Requirement: Form to update site name and description
-->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Site Settings | Flavour Academy</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;700&family=Open+Sans:wght@400;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/organiser.css">
    <link rel="stylesheet" href="/main.css">
</head>
<body>

    <nav class="navbar navbar-custom mb-4">
        <div class="container-fluid justify-content-end align-items-center" style="height: 100%;">
            <span class="navbar-brand mb-0">Flavour Academy</span>
        </div>
    </nav>

    <div class="mt-2 mb-4 home-btn-left">
        <a href="/organiser" class="btn btn-outline-light btn-sm">Back</a>
    </div>

    <div class="container mt-5" style="max-width: 600px;">
        <h1 class="mb-4 text-center">Site Settings Page</h1>

        <form method="POST" action="/organiser/settings" class="card card-accent p-4">
            <div class="mb-3">
                <label for="site_name" class="form-label">Site Name</label>
                <input 
                    type="text" 
                    class="form-control" 
                    id="site_name" 
                    name="site_name" 
                    value="<%= settings.site_name %>"
                    required
                    maxlength="100"
                >
            </div>

            <div class="mb-3">
                <label for="site_description" class="form-label">Site Description</label>
                <textarea 
                    class="form-control" 
                    id="site_description" 
                    name="site_description" 
                    rows="3"
                    required
                    maxlength="500"
                ><%= settings.site_description %></textarea>
            </div>

            <div class="d-flex gap-2">
                <button type="submit" class="btn btn-accent">Save Changes</button>
                <a href="/organiser" class="btn btn-outline-light">Cancel</a>
            </div>
        </form>
    </div>

    <div class="mb-5"></div>
</body>
</html>